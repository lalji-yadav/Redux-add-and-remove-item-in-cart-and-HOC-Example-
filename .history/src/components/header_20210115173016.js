import React from 'react';

const Header=(props)=> {

    console.warn('Heaser',props)

        return (
            <div>
                <h1> this is header  </h1>
            </div>
        );
    }

export default Header;
